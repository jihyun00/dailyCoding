#include <iostream>
#include <stdio.h>
#include <algorithm>

using namespace std;

char singer[200000];
char fan[200000];


int fanmeeting() {
    int res;


    return res;
}


int main(void) {
    int C;
    int rot;

    scanf("%d", &C);

    rot = C*2;

    for(;rot--;) {
        memset(singer, '0', sizeof(singer));
        memset(fan, '0', sizeof(fan));
        
        scanf("%s", signer);
        scanf("%s", fan);

        printf("%d\n", fanmeeting());
    }

    return 0;
}
